CREATE TABLE user (
    username varchar2(20);
    password varchar2(20);
);

CREATE TABLE dorms (
    name varchar2(20);
    address varchar2(50);
    rating INTEGER;
    dorm_id INTEGER;
);

CREATE TABLE tags (
    description varchar2(20);
    
);